{
  "name": "srm-bfhl",
  "version": "1.0.0",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",
    "vercel-build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && mv frontend/build public"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
